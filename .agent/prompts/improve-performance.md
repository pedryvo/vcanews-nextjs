# Prompt: Improve Performance

**Objective**: Optimize database queries and rendering in a specific module.

**Instructions**:
1. Analyze Prisma queries in the repository layer.
2. Add `select` fields to minimize data transfer.
3. Implement pagination (`skip`, `take`) where missing.
4. Check Client Components for unnecessary state and convert parts to Server Components if possible.
5. Verify changes with a build check.

Broadway
